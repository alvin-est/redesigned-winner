/* Inserted in <Outlet /> on browser route: / */

import React from 'react';

const HomePage = () => {
    return (
        <main className="container">
            <h2>Welcome to My Portfolio</h2>
            <p>This is the homepage of my portfolio. Stay tuned for more updates!</p>
        </main>
    );
};

export default HomePage;